// src/store/index.js (ou store.js dans src directement)
import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    // Définis l'état global ici
  },
  mutations: {
    // Définis les mutations ici
  },
  actions: {
    // Définis les actions ici
  },
  getters: {
    // Définis les getters ici
  }
});
